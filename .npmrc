# npm 10.9.x has an arborist bug that aborts workspace installs while resolving
# vitest's optional `jsdom` peer set ("Cannot read properties of null (reading
# 'edgesOut')"). Skipping peer resolution is the documented workaround and keeps
# installs reproducible across npm 10 and 11.
legacy-peer-deps=true
